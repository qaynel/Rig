{
  "name": "rig-mcp",
  "version": "5.0.0",
  "description": "MCP server that serves Rig's lazy-senior-dev instructions as a prompt and a tool.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "pretest": "[ -d node_modules ] || npm install --no-audit --no-fund",
    "test": "node --test ./test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.23.0"
  }
}
