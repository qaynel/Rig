---
date: 2026-08-21
source: agent
topics: authored-service-gate, specification-gate
decisions: D23
---

The intent owner flagged that D23 (the one-release exception to `AT-SHAPE-6`,
frozen in Gate 1 the same day) had not been carried into Gate 2. Their read,
from a review they'd run separately: the business-intent fix exists, but the
design document — the thing that actually drives what gets implemented and
tested — still pointed at the old, unscoped "review all 115 leaves" version of
the rule. On paper, that would still have kept `npm test` red on the 114
non-shipping leaves once the specification gate was actually built, regardless
of what Gate 1 now says. They asked for the narrowing to be carried into Gate 2
specifically as checkable acceptance criteria, not just prose, since a prose
mention that isn't wired into a traceability row or a release-gate condition
doesn't actually change what a future implementer builds.

Traced every place Gate 2 transcribes `AT-SHAPE-6` or the general release
boundary it interacts with:

- **§5.6 (Authored-service gate)** — defines the mechanical/semantic
  full-content check itself, still stated as all 115 leaves. This is the
  primary mechanism definition and needed the D23 qualifier directly.
- **§12.3 (Release gate), item 3** — "placeholder/contradiction checks ... pass
  with no unresolved item," unscoped. This is the actual load-bearing bug: item
  4 already correctly narrows to lint-format per D21/AD-31, but item 3 — which
  is exactly `AT-SHAPE-6` operating as a release-gate precondition — had no
  such narrowing and would have blocked release on the other 114 leaves' content
  regardless of item 4.
- **§17.2 (Release blockers), item 3** — "the §13 specification gate and
  complete 68-ID executable oracle exist and are green," which implicates
  `AT-SHAPE-6` without naming it. Same gap, different list.
- **§13's `AT-SHAPE-6` traceability row** — the most direct transcription,
  unchanged since it was written, describing an unscoped 115-leaf mechanical
  and semantic check with no reference to any release boundary.
- **AD-24** — already said catalogue leaves are staged per AD-31 so that
  filler/placeholder content blocks only that leaf and the complete-catalogue
  claim, not lint-format's release. This is about the *release claim*, a
  related but distinct thing from `AT-SHAPE-6`'s own gate-mechanism scope, so
  it got a short cross-reference to D23 rather than a rewrite.

Confirmed (not re-verified from scratch, but checked) that D22's `AT-CI-3`
narrowing is already consistent in both Gate 1 (`acceptance.md` line 494) and
Gate 2 (§13 row, line ~2347) — both carry the "repo-CI-applicable at their
active grade" qualifier. No change needed there; the intent owner's read that
this one was already resolved was correct.

Edited §5.6, §12.3 item 3, §17.2 item 3, AD-24, and the §13 `AT-SHAPE-6` row to
state the same one-release scoping D23 froze in Gate 1, each phrased as an
observable, checkable condition (what the gate evaluates, against which leaf,
for how long) rather than a narrative aside. Bumped the candidate to v0.9,
re-pinned Gate 1's digests (07afa02f…/1d9b7a4e…, the D23 bytes), and updated
the version-history note. Did not touch the six other open round-5 findings
(evidence-gate consistency, traceability rows pointing at non-real tests,
rollback behavior, recovery-credential verification, status reporting, stale
version references) — unrelated to this scoping fix, still open. Did not mark
Gate 2 `FROZEN` and did not edit any Gate 1 file.
