---
date: 2026-08-24
source: intent owner
topics: user-global-writes, host-and-ci-coverage
decisions:
---

Yeah, write up an MCP for the open clock global file. Also, I mean if the user wants it, they can mean if the user wants it in the during installation if the user wants it, we can install it on the global context also if needed, so do that so yeah I mean when setting up the if if the user selects clone and the and the MCP thing and all comes up then we can give them an option that just want them saying that kid this would be on the global context in order a rap context not a repository context. It would be a global context with that warning. I think we can install it globally if they use their wishes so
