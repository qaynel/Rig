# RIG-104 — Unify the legacy `renderers.js` MCP path with the catalogue materializer

**Status: RESOLVED (2026-08-24) — landed, gate green.**
Ticket: RIG-104 on [[Tickets]].

## Landed

`rig/lib/mcp-hosts.js` (new) is the single `{ disposition, autoWrite, file,
key }` table, derived from `host-capabilities.js` `REGISTRY` with two named,
documented overrides (antigravity manual-only per RIG-105; codewhale's shipped
`DEEPSEEK_MCP_CONFIG` repo-redirect). Both `renderers.js` (legacy Basic path)
and the catalogue descriptor path now read it — `HOST_TIER`/`HOST_FILES` are
gone. The catalogue host contracts and selected-host summaries now expose the
same MCP key (`mcpServers`, `servers`, `mcp`, `mcp.servers`, or
`[mcp_servers]`) instead of forcing any downstream caller to infer the shape
again. A shared `mergeMcpEntry`/`setAtPath` writer places every JSON-shaped
host's entry at its governing dotted key, proven idempotent and
unrelated-entry-preserving per shape (flat `mcpServers`, copilot
`servers`+`inputs`, opencode `mcp`, OpenClaw's nested `mcp.servers`) plus the
Codex TOML block, by `tests/basic-mcp-merge.test.js`. `pi`'s renderer is
deleted; the disposition gate now preserves + guides a pre-existing user file
for any unsupported/manual host (AT-HOST-5, `tests/basic-renderers.test.js`,
covering the legacy path the frozen catalogue-only oracle test didn't reach).
Network-capable (http) MCP entries are evaluated through the same
`evaluateAction` engine and the same active `.rig/network-policy.json` as
shell/web, recorded on the receipt, with a parity test. Two real path/shape
divergences surfaced and were reconciled in favor of the already-shipped,
tested behavior: OpenClaw's registry metadata was corrected to
`.openclaw/openclaw.json` + `mcp.servers` (was bare `openclaw.json` +
`mcpServers`, unused by any code); `docs/agent-portability.md`'s `rig-mcp`
shape table corrected to match. Full gate green (root/pi-extension/rig-mcp).
See [reasoning](../reasoning/2026-08-24-rig-104-mcp-unification.md).

`copilot-cli` is now wired too (added same day after owner pushback): it's
not one of the §3.1 unresolved-conflict hosts, `host-coverage-spec` names its
shape confidently and with a citation, so it gets a real renderer like every
other cited `mcp: 'repo'` host rather than staying note-only by omission.

---

**Original status: DESIGNED — implementation-ready (single source of truth + merge contract).**

## Problem
`host-coverage-spec §3.2` records that the legacy Basic `renderers.js`/`HOST_FILES`
path and the à-la-carte catalogue MCP materialization were "deliberately left
untouched and deferred," which it calls incompatible with AT-HOST-5. Two
independent code paths can emit MCP config with divergent per-host key/shapes and
no single safe merge/preflight contract.

## Findings
- `renderers.js` hardcodes per-host key/shape inline: copilot `servers`, opencode
  `mcp`+`type`, codex TOML `[mcp_servers]`, else `mcpServers` — each embedded in
  a separate `renderX` function, plus a Tier A/B/C map and a `CREDENTIAL_SAFETY`
  map.
- §3.2 already researched the canonical data: `mcp` disposition
  (`repo`/`user_global`/`unsupported`), `surfaces.mcp` path, and
  `surfaces.mcp_key` overrides. That is the source of truth the code should read
  instead of re-encoding it twice.

## Solution (one disposition, both paths)
1. **Extract a single `mcp-hosts.js` table**: `{ host: { disposition, file,
   key, shape } }` populated from §3.2. Both `renderers.js` and the catalogue
   materializer import it; delete the inline per-host branches.
2. **One merge writer.** A single `mergeMcpEntry(existingObj, host, server,
   variant)` that: places the entry at the host's `key`, is idempotent
   (re-apply = no-op), and preserves every unrelated server for each shape
   (`mcpServers` object, copilot `servers`+`inputs`, opencode `mcp`, codex TOML
   block). This replaces the divergent `jsonFile`/`appendTomlBlock` call sites.
3. **Preflight = disposition gate.** `unsupported` → emit nothing, preserve +
   advise ([[RIG-103]]); `user_global` → advisory note only unless the
   user-global writer contract ([[RIG-111]]) applies; `repo` → merge.
4. **Policy parity.** Route network-capable MCP entries through the same active
   `.rig/network-policy.json` check as shell/web (MCP is never an enforcement
   bypass) with a test — mirrors §4 "one policy across network surfaces."

## Acceptance mapping
- One governing disposition + key map drives both install paths → step 1.
- Idempotent merge preserving unrelated servers for every shape → step 2 + a
  per-shape merge test.
- Network MCP obeys active policy, with a test → step 4.
- AT-HOST-5 coverage → shared with [[RIG-103]].

## Sequencing
Do [[RIG-103]] as the first slice (pi), then generalize the map here. Feeds
[[RIG-101]] (rig-mcp registration reads the same map) and [[RIG-111]]
(user-global shapes).
