# RIG-109 — Test the runtime through its shipping path, not by direct `require`

**Status: COMMITTED (2026-08-24) — landed on this branch.**
Ticket: RIG-109 on [[Tickets]].

## Final recovery checkpoint
- **Fresh-context result:** No new code was required. RIG-107 already supplied
  the installed command and end-to-end lint-format tracer; RIG-108 added the
  runtime caller graph and additional production-seam cases.
- **Focused evidence:** the active-runtime lint-format entrypoint and installed
  commit-validation cases pass from the public `.rig/bin/rig` seam.
- **Integrated evidence:** the immediately preceding full gate verifies the
  signed 68-case oracle and passes 414 root Node tests, 15 pi-extension tests,
  and 6 rig-mcp tests.
- **Review:** Spec, correctness, architecture, and coverage reconciliation found
  no remaining acceptance gap. Adding another equivalent end-to-end test would
  be duplication, not evidence.
- **Next:** Commit with the other ready work when the owner chooses the boundary.

## Problem
The signed oracle binds behavior with `require(file)[name]`, so 68/68 green is
(per the testing-strategy topic) "behavior at a seam the product does not use."
Even correct modules aren't proven reachable.

## Findings
- The oracle's value is provenance/immutability, not reachability — it verifies
  the *function* behaves, not that any shipping flow calls it.
- There is currently no installed entrypoint to test through ([[RIG-107]]); once
  `.rig/bin/rig` exists, an end-to-end test can drive real behavior.

## Solution (complement, don't replace, the oracle)
1. After [[RIG-107]] lands `.rig/bin/rig`, add at least one **end-to-end** test:
   install `--with-runtime` into a temp repo, run `.rig/bin/rig inspect →
   recommend → plan → apply → check` for the lint-format leaf, and assert the
   observable result (bindings written, check runs read-only, exit codes). This
   asserts the *product* works, not just the seam.
2. Keep the direct-`require` oracle as the frozen contract; the e2e test is
   additive evidence. Document in `topics/testing-strategy.md` that the
   "green oracle is not evidence the product works" caveat is now closed by the
   e2e coverage, and link the test.
3. Fold in the [[RIG-108]] `release-evidence.js`-through-`review-receipt.js` test
   as a second shipping-path example.

## Dependency status
[[RIG-107]] and [[RIG-108]] are resolved in the same worktree. This ticket is
their acceptance reconciliation and introduces no separate code diff.

## Acceptance resolution
- **Installed end to end:** the bootstrap regression installs active delivery,
  invokes `.rig/bin/rig` to plan and apply the lint-format leaf, executes its
  installed check, and asserts the real service binding and check runner.
- **Caveat closed honestly:** the testing-strategy topic now records that total
  unreachability is superseded by installed-path evidence, while retaining the
  narrower truth that one tracer does not prove every behavior. A production
  caller graph plus additional shipping-path cases prevent regression to the
  original library-only state.

## Acceptance

- Active-runtime bootstrap plans, applies, and checks through the installed `.rig/bin/rig` command.
  → tests/rig-bootstrap.test.js::Tier 1 --with-runtime installs and surfaces a working catalogue entrypoint
- The installed check dispatches the real lint-format binding rather than a direct require.
  → tests/advanced-lint-format.test.js::source check command dispatches the same real installed binding
- A production caller graph prevents regression to library-only modules.
  → tests/runtime-caller-graph.test.js::every runtime library module has a production caller

