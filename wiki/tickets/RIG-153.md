# RIG-153 — No frozen instrument exists to measure adaptation quality

**Class: PROCESS / TOOLING GAP.**
**Status: DONE (2026-08-31) — GitHub #130 — found during Path A install-bug investigation.**
Ticket: RIG-153 on [[Tickets]].

**Done:** option A (the instrument) is delivered and frozen (`592f4eed`). The
re-baseline *run* (option B) is carved out as [[RIG-156]] — deferred, low
priority, held until Path A and Path B are fully decided.
[[reasoning/2026-08-31-rig-153-close-option-b-deferred]]

## Problem

The only adaptation-quality measurement on record is
[[reasoning/2026-08-30-adaptation-eval-claude-task-master]]: one holistic
Sonnet pass, self-reported ±5, scoring +12/100 — and it scored a
`--with-runtime` install, not the default product
([[reasoning/2026-08-30-office-hours-path-a-path-b-scoping]] already
corrected this framing; confirmed again independently in
[[reasoning/2026-08-30-path-a-bug-investigation]] §0). No rubric file,
fixed axis list, weight scheme, pinned model, or clean-checkout procedure
exists anywhere in the repo. Path A's own stated goal — "fix the concrete
install bugs and freeze a real measurement instrument, then re-baseline" —
cannot produce a comparable number without one.

## Required instrument shape

- **Fixed axes with weights**, drawn from the existing eval's own
  dimensions (they already proved discriminating): config preserved (no
  clobber/data loss — weighted highest, it's correctness not taste),
  breakage/dangling-reference count (exactly what [[RIG-149]]/[[RIG-150]]/
  [[RIG-151]]/[[RIG-152]] are instances of — mechanically detectable
  without a model call), signal-vs-noise (relevant-skill count and byte
  weight against total installed), net capability added, reversibility
  (uninstall round-trip). Breakage-count and config-preservation weighted
  above taste-driven axes.
- **A pinned model and fixed prompt template** (or documented sampling
  temperature) so repeat runs are comparable.
- **A clean-checkout procedure**: fresh clone/copy of the target repo, one
  documented `bootstrap.sh` invocation logged alongside the score,
  `git status`/`git diff` captured as raw evidence — the existing eval
  already does this part; codify it as a required step, not an incidental
  choice.
- **Running the default install, not `--with-runtime`** — matching what a
  real user gets. A `--with-runtime` run needs its own separately labeled
  rubric pass if it's ever evaluated.

The breakage-count axis can be partly mechanical immediately: a pre-eval
lint pass that greps/tree-diffs for the exact defect shapes [[RIG-149]]
through [[RIG-152]] catch could regression-test this axis before any
model call, and before the next eval run.

No score is proposed here; this ticket specs the instrument, not a
re-score.

## Location

No code exists yet. Candidate home: a new `scripts/eval-adaptation.js` (or
similar) plus a rubric doc under `wiki/specs/` or `wiki/gate2/`.

## Acceptance

Option A — the instrument. Option B (the re-baseline *run*) is not part of this
ticket; it moved to [[RIG-156]], deferred until Path A and Path B are fully
decided. Context and the five readiness blockers:
[[reasoning/2026-08-31-rig-153-close-option-b-deferred]].

- `tests/installed-router-hygiene.test.js` now covers all six RIG-148→152 defect
  shapes and is green in `npm test`; the RIG-149 block adds the `rig`→`rig-rig`
  self-collision guard check (tests 1–4 for RIG-124.2 / RIG-151 / RIG-152 /
  RIG-150 pre-existed).
  → tests/installed-router-hygiene.test.js::RIG-149 — prefix self-collision guard is present for the rig router skill
- The sixth block asserts the manifest ships an `ensure_gitignore_block` for
  Rig-owned install paths — the last of the six defect shapes.
  → tests/installed-router-hygiene.test.js::RIG-148 — manifest writes a gitignore block for Rig-owned install paths
- `wiki/specs/adaptation-measurement-rubric.md` exists and is frozen: 2 hard
  gates (G1 Config Preserved, G2 Zero Breakages, MET/UNMET), 3 equal-weight
  graded axes (A1 Signal-vs-Noise, A2 Net Capability Added, A3 Reversibility,
  fail/partial/pass), per-axis profile format (not a scalar), the
  low-confidence-aggregate caveat, the clean-checkout procedure, the prompt
  template, the model-EOL re-pin step, a pinned dated `claude-sonnet-5`
  snapshot, ≥3 runs with mean and observed spread, and the
  eyaltoledano/claude-task-master anchor recording both SHAs per run.
  → manual: the rubric is a frozen wiki spec (wiki/specs/adaptation-measurement-rubric.md) verified by human review against the locked RIG-153 decisions

## Evidence

- Technical spec: [[reasoning/2026-08-31-rig-153-instrument-spec.md]]
- Rubric document: [[specs/adaptation-measurement-rubric.md]]
- Close-out ruling: [[reasoning/2026-08-31-rig-153-close-option-b-deferred]]
- Original defect inventory: [[reasoning/2026-08-30-path-a-bug-investigation]] §5–6
- Grilling oracle: [[reasoning/2026-08-31-routing-hygiene-oracle.md]]
