# RIG-121 — Refresh drifted wiki specs to match shipped reality

**Status: RESOLVED (landed).**
Ticket: RIG-121 on [[Tickets]].

## Problem
Per CLAUDE.md a drifted wiki is a defect. Two specs carried stale claims.

## Findings (verified 2026-08-24)
- `specs/lint-format-roadmap.md` said *"Still placeholders. 428 `TODO(Slice 10)`
  files remain."* Ground truth: **zero** catalogue-leaf placeholder files exist.
  `grep -rl "TODO(Slice 10)"` matches only 13 *prose* references in wiki/docs,
  none of them leaf fragments. `delivery-plan.md` step 5 records "805 files, zero
  placeholders."
- `specs/product-spec.md` gap table listed as open two items already resolved:
  `publish.yml` (now deleted — only `test.yml` remains) and the `main`-vs-`master`
  branch gate (`test.yml` now runs `branches: ['**']`). Its R3 row and `RIG_REPO`
  stub still named the dead `agentic-harness-demo` repo.

## Solution (landed)
- `lint-format-roadmap.md`: rewrote the "Other 114 leaves" row to
  "Authored at Policy grade, no placeholders" with a dated *superseded* note
  citing delivery-plan step 5, and pointed forward to [[RIG-116]] for the
  Context/Evidence promotion.
- `product-spec.md`: struck the `publish.yml` and `main`/`master` gap rows with
  dated **Resolved** annotations; struck the R3 repo-identity gap row and prose
  with a dated **Resolved (RIG-117)** note and recorded the open `qaynel/Rig`
  vs `qaynel/Rig-v0.1` naming decision; fixed the `RIG_REPO` default to the real
  repo.

## Not changed (deliberate)
`lint-format-roadmap.md` is `status: historical` and already flags itself as
superseded by delivery-plan/status; I corrected the one factual count the ticket
named rather than rewriting the historical narrative. Remaining "open" lines in
that file were spot-checked and still reflect the branch (authored-service gate,
§7.6 completeness).

## Verification
`npm test` green (secret/rule/version checks unaffected). Board card links here.
