# RIG-156 — (Low priority / deferred) Re-baseline the adaptation score against post-fix Rig (RIG-153 option B)

**Class: MEASUREMENT / PROCESS.**
**Status: OPEN (2026-08-31) — deferred, low priority — carved out of [[RIG-153]] on close.**
Ticket: RIG-156 on [[Tickets]].

## Problem

The only adaptation-quality number on record is the +12/100 holistic pass in
[[reasoning/2026-08-30-adaptation-eval-claude-task-master]]. [[RIG-153]] froze
a repeatable measurement instrument for it — the six hygiene tests in
`tests/installed-router-hygiene.test.js` plus the model-judged rubric at
[[specs/adaptation-measurement-rubric.md]] (2 hard gates, 3 graded axes,
per-axis profile, clean-checkout procedure, prompt template). RIG-153 was
scoped to build the instrument, not to run it. This ticket is the run: produce
a post-fix per-axis profile and compare it against the +12 baseline.

## Why deferred

- The six hygiene tests already mechanically prove the RIG-148..152 defect
  shapes are fixed and run in CI. A re-baseline today would mostly flip the G2
  gate UNMET→MET and move A1/A2/A3 barely at all — low information.
- The *interesting* re-baseline is after adaptive integration (reconcile with
  the target's existing config, prune to stack, personalize content — the
  "single highest-value change to Rig" from the +12 eval) is built. That is a
  Path B question, not settled.

Hold until Path A and Path B are fully decided and cleared. Context and
five-blocker analysis: [[reasoning/2026-08-31-rig-153-close-option-b-deferred]].

## Blockers / preconditions

1. **Clean target copy in-workspace.** A fresh `986de06` checkout of
   eyaltoledano/claude-task-master at `inspo/claude-task-master-main/` with zero
   prior Rig state. The current `~/projects/claude-task-master-main` copy is
   polluted with run-1 install output and is not in this workspace.
2. **Path A + Path B direction decided.** Especially whether adaptive
   integration (prune-to-stack, reconcile-with-existing-config) is in scope —
   that is what makes the number worth measuring.
3. **Rig SHA pinned as the re-baseline subject.** Frozen instrument anchor
   `38493a9b` (qa-prod-v5), current HEAD, or post qa-prod-v6→prod merge — each
   yields a different number. Default recommendation: whatever a user installs
   from `prod` at run time, with the delta from `38493a9b` noted.
4. **Judge operator + interface decided.** The rubric's judge is human-operated
   (no API-key-wired script). Decide who runs it and where.
5. **Model pin string source.** A verbatim dated snapshot (e.g.
   `claude-sonnet-5-20251022`) from the API response header or an interface that
   exposes it — `claude --version` gives the CLI version, not the model.

## Non-goals

- No change to `wiki/specs/adaptation-measurement-rubric.md` — it is frozen and
  its §Clean-Checkout Procedure is already turnkey.
- No `--with-runtime` run — that needs its own separately labelled rubric pass.
- No re-derivation of the +12 baseline — it is immutable history.

## Acceptance

_Not yet implemented — deferred._

- ≥3 clean-checkout runs logged, each with a full per-axis profile (G1/G2
  MET/UNMET; A1/A2/A3 fail/partial/pass), mean and observed spread per axis,
  Rig branch SHA and target commit SHA recorded.
- A dated reasoning trace under `wiki/reasoning/` compares the new per-axis
  profile against the v0.1 baseline (+12/100).
- This ticket header updated with the post-fix result; board card moved to Done.

## Evidence

_None yet — deferred._

- Instrument: [[RIG-153]], [[specs/adaptation-measurement-rubric.md]],
  [[reasoning/2026-08-31-rig-153-instrument-spec.md]]
- Baseline: [[reasoning/2026-08-30-adaptation-eval-claude-task-master]]
- Close-out context: [[reasoning/2026-08-31-rig-153-close-option-b-deferred]]
