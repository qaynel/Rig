# RIG-120 — Finish the release ceremony and cut v5.0.0

**Status: DEFERRED — needs owner action (cannot be done by an agent).**
Ticket: RIG-120 on [[Tickets]].

## Problem
The release is blocked on owner-controlled inputs (status.md "Owner-controlled
release inputs"): a fresh independent review receipt bound to the exact PR
worktree, the intent owner's signer-class attestation on the frozen Gate 1
signer file, and the explicit `v5.0.0` tag/publish (never an implicit side
effect). Until these land there is no installable release, only a source
checkout.

## Why this genuinely needs you
Every remaining input requires the **owner's private signing key / authority**,
which an agent does not hold:
1. **Fresh independent review receipt** against the exact PR worktree — the
   release-review wrapper starts a fresh reviewer process and refuses incomplete
   or failing evidence; it must run on the final bytes.
2. **Signer-class attestation** on the frozen Gate 1 signer file — an
   owner-authorized re-signing ceremony (`ssh-keygen -Y sign`), not delegable.
3. **The `v5.0.0` tag + publish** — an explicit owner operation; CI's
   `check-versions.js` will enforce that the tag equals the pinned version.

## What I can/did prepare toward it
- The branch's full gate is **green** (`npm test`: 380/380 + 15/15) on the
  current bytes, including the new identity guard ([[RIG-117]]).
- Flagged the one naming reconciliation that must happen before publish:
  `qaynel/Rig` (install.sh/README) vs `qaynel/Rig-v0.1` (remote) — see [[RIG-117]].
- Several other tickets ([[RIG-102]], [[RIG-112]], [[RIG-115]], [[RIG-108]])
  require the same re-signing ceremony; if you're opening the ceremony anyway,
  batch their oracle changes into it.

## Your move
When ready: reconcile the repo name, run the fresh independent review on the PR
worktree, perform the re-signing ceremony (folding in any batched oracle
changes), rerun the full gate on the final bytes, then tag + publish `v5.0.0`.
