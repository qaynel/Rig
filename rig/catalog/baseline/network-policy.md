# Rig policy guide

The explanatory policy guide is `.rig/network-rules.md`. The authoritative
machine-readable candidate is `.rig/network-policy.json`; prose never overrides
that JSON or the activated snapshot.
