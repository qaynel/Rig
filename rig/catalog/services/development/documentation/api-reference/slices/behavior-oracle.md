# development.documentation.api-reference - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming api reference result from a real policy violation. It activates `api-reference-source-code-boundary`, `api-reference-documentation-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
