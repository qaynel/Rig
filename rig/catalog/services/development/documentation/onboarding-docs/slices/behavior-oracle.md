# development.documentation.onboarding-docs - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming onboarding docs result from a real policy violation. It activates `onboarding-docs-source-code-boundary`, `onboarding-docs-documentation-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
