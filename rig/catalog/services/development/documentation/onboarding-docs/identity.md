# development.documentation.onboarding-docs - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

onboarding docs is the sole catalogue owner of `documentation.onboarding-docs`. It must not absorb other documentation concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `documentation.onboarding-docs`, the result is a named nonzero coverage gap.

Acceptance target: fixture:development.documentation.onboarding-docs:policy-boundary. Given source-code is present and documentation.onboarding-docs is selected, pass only when the repository binding evaluates documentation.onboarding-docs and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent documentation concern.
