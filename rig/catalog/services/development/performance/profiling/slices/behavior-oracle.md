# development.performance.profiling - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming profiling result from a real policy violation. It activates `profiling-source-code-boundary`, `profiling-performance-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
