# development.architecture.tech-debt - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `tech-debt-architecture-context` evaluates `architecture.tech-debt`. Given architecture.tech-debt has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent architecture concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:development.architecture.tech-debt:repository-context`, and adjacent architecture ownership stays outside the verdict.
