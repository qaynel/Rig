# development.architecture.tech-debt - slice property-floor

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `property-floor` slice holds ownership at `architecture.tech-debt` and prevents none from being silently absorbed. It activates `tech-debt-source-code-boundary`, `tech-debt-evidence-receipt` and reports a named coverage gap when its repository binding or required dependency is unavailable.
