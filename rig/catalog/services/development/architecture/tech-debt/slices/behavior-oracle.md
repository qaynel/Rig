# development.architecture.tech-debt - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming tech debt result from a real policy violation. It activates `tech-debt-source-code-boundary`, `tech-debt-architecture-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
