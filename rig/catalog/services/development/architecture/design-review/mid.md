# development.architecture.design-review - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `design-review-architecture-context` evaluates `architecture.design-review`. Given architecture.design-review has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent architecture concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:development.architecture.design-review:repository-context`, and adjacent architecture ownership stays outside the verdict.
