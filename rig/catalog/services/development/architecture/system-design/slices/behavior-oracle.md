# development.architecture.system-design - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming system design result from a real policy violation. It activates `system-design-source-code-boundary`, `system-design-architecture-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
