# development.code-creation.api-client-generation - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

api client generation is the sole catalogue owner of `code-creation.api-client-generation`. It must not absorb other code creation concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `code-creation.api-client-generation`, the result is a named nonzero coverage gap.

Acceptance target: fixture:development.code-creation.api-client-generation:policy-boundary. Given source-code is present and code-creation.api-client-generation is selected, pass only when the repository binding evaluates code-creation.api-client-generation and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent code creation concern.
