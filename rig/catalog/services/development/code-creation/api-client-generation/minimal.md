# development.code-creation.api-client-generation - minimal

Grade: minimal. Assurance is Policy grade generic baseline practice and is not tailored to the installing repository.

`api-client-generation-source-code-boundary` owns `code-creation.api-client-generation`. Given source-code is present and code-creation.api-client-generation is selected, it passes only when the repository binding evaluates code-creation.api-client-generation and returns zero. It fails when the binding is missing, unreadable, exits nonzero, or claims an adjacent code creation concern. Evidence is recorded against `fixture:development.code-creation.api-client-generation:policy-boundary`; absence never becomes a pass.
