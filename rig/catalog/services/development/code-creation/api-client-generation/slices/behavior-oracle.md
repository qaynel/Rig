# development.code-creation.api-client-generation - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming api client generation result from a real policy violation. It activates `api-client-generation-source-code-boundary`, `api-client-generation-code-creation-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
