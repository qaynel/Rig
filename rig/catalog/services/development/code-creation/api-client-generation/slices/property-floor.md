# development.code-creation.api-client-generation - slice property-floor

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `property-floor` slice holds ownership at `code-creation.api-client-generation` and prevents none from being silently absorbed. It activates `api-client-generation-source-code-boundary`, `api-client-generation-evidence-receipt` and reports a named coverage gap when its repository binding or required dependency is unavailable.
