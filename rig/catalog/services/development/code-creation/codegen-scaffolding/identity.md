# development.code-creation.codegen-scaffolding - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

codegen scaffolding is the sole catalogue owner of `code-creation.codegen-scaffolding`. It must not absorb other code creation concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `code-creation.codegen-scaffolding`, the result is a named nonzero coverage gap.

Acceptance target: fixture:development.code-creation.codegen-scaffolding:policy-boundary. Given source-code is present and code-creation.codegen-scaffolding is selected, pass only when the repository binding evaluates code-creation.codegen-scaffolding and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent code creation concern.
