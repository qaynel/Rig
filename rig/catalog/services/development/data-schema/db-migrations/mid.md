# development.data-schema.db-migrations - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `db-migrations-data-schema-context` evaluates `data-schema.db-migrations`. Given data-schema.db-migrations has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent data schema concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:development.data-schema.db-migrations:repository-context`, and adjacent data schema ownership stays outside the verdict.
