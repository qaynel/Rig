# development.data-schema.schema-evolution - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming schema evolution result from a real policy violation. It activates `schema-evolution-source-code-boundary`, `schema-evolution-data-schema-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
