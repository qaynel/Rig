# development.data-schema.schema-evolution - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

schema evolution is the sole catalogue owner of `data-schema.schema-evolution`. It must not absorb other data schema concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `data-schema.schema-evolution`, the result is a named nonzero coverage gap.

Acceptance target: fixture:development.data-schema.schema-evolution:policy-boundary. Given source-code is present and data-schema.schema-evolution is selected, pass only when the repository binding evaluates data-schema.schema-evolution and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent data schema concern.
