# development.repo-hygiene.issue-pr-triage - maximal

Grade: maximal (Evidence target). Until a rerunnable receipt exists, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal and mid checks pass, `issue-pr-triage-evidence-receipt` covers `repo-hygiene.issue-pr-triage`. Given the repo-hygiene.issue-pr-triage binding and its inputs are unchanged, it passes only when a second run reproduces the verdict and input/output digests; it fails when the receipt is stale, incomplete, or cannot be rerun. The receipt at `fixture:development.repo-hygiene.issue-pr-triage:evidence-replay` names the binding, inputs, outcome, and current digest.
