# development.repo-hygiene.status-updates - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `status-updates-repo-hygiene-context` evaluates `repo-hygiene.status-updates`. Given repo-hygiene.status-updates has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent repo hygiene concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:development.repo-hygiene.status-updates:repository-context`, and adjacent repo hygiene ownership stays outside the verdict.
