# development.dependency-management.upgrades-migration - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

upgrades migration is the sole catalogue owner of `dependency-management.upgrades-migration`. It must not absorb other dependency management concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `dependency-management.upgrades-migration`, the result is a named nonzero coverage gap.

Acceptance target: fixture:development.dependency-management.upgrades-migration:policy-boundary. Given source-code is present and dependency-management.upgrades-migration is selected, pass only when the repository binding evaluates dependency-management.upgrades-migration and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent dependency management concern.
