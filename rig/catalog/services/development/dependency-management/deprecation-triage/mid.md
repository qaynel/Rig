# development.dependency-management.deprecation-triage - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `deprecation-triage-dependency-management-context` evaluates `dependency-management.deprecation-triage`. Given dependency-management.deprecation-triage has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent dependency management concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:development.dependency-management.deprecation-triage:repository-context`, and adjacent dependency management ownership stays outside the verdict.
