# development.dependency-management.deprecation-triage - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

deprecation triage is the sole catalogue owner of `dependency-management.deprecation-triage`. It must not absorb other dependency management concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `dependency-management.deprecation-triage`, the result is a named nonzero coverage gap.

Acceptance target: fixture:development.dependency-management.deprecation-triage:policy-boundary. Given source-code is present and dependency-management.deprecation-triage is selected, pass only when the repository binding evaluates dependency-management.deprecation-triage and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent dependency management concern.
