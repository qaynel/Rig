# development.dependency-management.deprecation-triage - maximal

Grade: maximal (Evidence target). Until a rerunnable receipt exists, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal and mid checks pass, `deprecation-triage-evidence-receipt` covers `dependency-management.deprecation-triage`. Given the dependency-management.deprecation-triage binding and its inputs are unchanged, it passes only when a second run reproduces the verdict and input/output digests; it fails when the receipt is stale, incomplete, or cannot be rerun. The receipt at `fixture:development.dependency-management.deprecation-triage:evidence-replay` names the binding, inputs, outcome, and current digest.
