# development.debugging.root-cause-analysis - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `root-cause-analysis-debugging-context` evaluates `debugging.root-cause-analysis`. Given debugging.root-cause-analysis has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent debugging concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:development.debugging.root-cause-analysis:repository-context`, and adjacent debugging ownership stays outside the verdict.
