# development.debugging.structured-debugging - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming structured debugging result from a real policy violation. It activates `structured-debugging-source-code-boundary`, `structured-debugging-debugging-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
