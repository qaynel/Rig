# development.debugging.structured-debugging - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

structured debugging is the sole catalogue owner of `debugging.structured-debugging`. It must not absorb other debugging concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `debugging.structured-debugging`, the result is a named nonzero coverage gap.

Acceptance target: fixture:development.debugging.structured-debugging:policy-boundary. Given source-code is present and debugging.structured-debugging is selected, pass only when the repository binding evaluates debugging.structured-debugging and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent debugging concern.
