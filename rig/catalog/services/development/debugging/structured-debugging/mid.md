# development.debugging.structured-debugging - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `structured-debugging-debugging-context` evaluates `debugging.structured-debugging`. Given debugging.structured-debugging has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent debugging concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:development.debugging.structured-debugging:repository-context`, and adjacent debugging ownership stays outside the verdict.
