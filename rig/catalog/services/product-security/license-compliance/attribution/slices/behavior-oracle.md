# product-security.license-compliance.attribution - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming attribution result from a real policy violation. It activates `attribution-source-code-boundary`, `attribution-license-compliance-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
