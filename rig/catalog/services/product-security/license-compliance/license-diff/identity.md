# product-security.license-compliance.license-diff - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

license diff is the sole catalogue owner of `license-compliance.license-diff`. It must not absorb other license compliance concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `license-compliance.license-diff`, the result is a named nonzero coverage gap.

Acceptance target: fixture:product-security.license-compliance.license-diff:policy-boundary. Given source-code is present and license-compliance.license-diff is selected, pass only when the repository binding evaluates license-compliance.license-diff and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent license compliance concern.
