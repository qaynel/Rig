# product-security.secrets.rotation-reminder - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming rotation reminder result from a real policy violation. It activates `rotation-reminder-source-code-boundary`, `rotation-reminder-secrets-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
