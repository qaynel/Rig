# product-security.red-team.abuse-resistance - slice property-floor

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `property-floor` slice holds ownership at `red-team.abuse-resistance` and prevents none from being silently absorbed. It activates `abuse-resistance-source-code-boundary`, `abuse-resistance-evidence-receipt` and reports a named coverage gap when its repository binding or required dependency is unavailable.
