# product-security.red-team.adversarial-simulation - slice property-floor

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `property-floor` slice holds ownership at `red-team.adversarial-simulation` and prevents none from being silently absorbed. It activates `adversarial-simulation-source-code-boundary`, `adversarial-simulation-evidence-receipt` and reports a named coverage gap when its repository binding or required dependency is unavailable.
