# testing.e2e.browser-automation - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `browser-automation-e2e-context` evaluates `e2e.browser-automation`. Given e2e.browser-automation has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent e2e concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.e2e.browser-automation:repository-context`, and adjacent e2e ownership stays outside the verdict.
