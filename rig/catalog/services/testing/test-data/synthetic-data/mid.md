# testing.test-data.synthetic-data - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `synthetic-data-test-data-context` evaluates `test-data.synthetic-data`. Given test-data.synthetic-data has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent test data concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.test-data.synthetic-data:repository-context`, and adjacent test data ownership stays outside the verdict.
