# testing.test-data.synthetic-data - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

synthetic data is the sole catalogue owner of `test-data.synthetic-data`. It must not absorb other test data concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `test-data.synthetic-data`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.test-data.synthetic-data:policy-boundary. Given source-code is present and test-data.synthetic-data is selected, pass only when the repository binding evaluates test-data.synthetic-data and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent test data concern.
