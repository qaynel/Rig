# testing.test-data.anonymization - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

anonymization is the sole catalogue owner of `test-data.anonymization`. It must not absorb other test data concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `test-data.anonymization`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.test-data.anonymization:policy-boundary. Given source-code is present and test-data.anonymization is selected, pass only when the repository binding evaluates test-data.anonymization and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent test data concern.
