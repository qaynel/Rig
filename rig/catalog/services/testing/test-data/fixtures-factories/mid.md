# testing.test-data.fixtures-factories - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `fixtures-factories-test-data-context` evaluates `test-data.fixtures-factories`. Given test-data.fixtures-factories has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent test data concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.test-data.fixtures-factories:repository-context`, and adjacent test data ownership stays outside the verdict.
