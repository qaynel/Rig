# testing.mutation.language-adapter - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

language adapter is the sole catalogue owner of `mutation.language-adapter`. It must not absorb other mutation concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `mutation.language-adapter`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.mutation.language-adapter:policy-boundary. Given source-code is present and mutation.language-adapter is selected, pass only when the repository binding evaluates mutation.language-adapter and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent mutation concern.
