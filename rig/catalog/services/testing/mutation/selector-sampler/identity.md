# testing.mutation.selector-sampler - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

selector sampler is the sole catalogue owner of `mutation.selector-sampler`. It must not absorb other mutation concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `mutation.selector-sampler`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.mutation.selector-sampler:policy-boundary. Given source-code is present and mutation.selector-sampler is selected, pass only when the repository binding evaluates mutation.selector-sampler and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent mutation concern.
