# testing.mutation.equivalent-mutant-detector - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

equivalent mutant detector is the sole catalogue owner of `mutation.equivalent-mutant-detector`. It must not absorb other mutation concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `mutation.equivalent-mutant-detector`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.mutation.equivalent-mutant-detector:policy-boundary. Given source-code is present and mutation.equivalent-mutant-detector is selected, pass only when the repository binding evaluates mutation.equivalent-mutant-detector and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent mutation concern.
