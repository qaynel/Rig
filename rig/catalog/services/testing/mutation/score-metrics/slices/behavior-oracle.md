# testing.mutation.score-metrics - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming score metrics result from a real policy violation. It activates `score-metrics-source-code-boundary`, `score-metrics-mutation-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
