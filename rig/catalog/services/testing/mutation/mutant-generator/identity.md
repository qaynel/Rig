# testing.mutation.mutant-generator - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

mutant generator is the sole catalogue owner of `mutation.mutant-generator`. It must not absorb other mutation concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: `[object Object]`. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `mutation.mutant-generator`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.mutation.mutant-generator:policy-boundary. Given source-code is present and mutation.mutant-generator is selected, pass only when the repository binding evaluates mutation.mutant-generator and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent mutation concern.
