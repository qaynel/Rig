# testing.integration.cross-module - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `cross-module-integration-context` evaluates `integration.cross-module`. Given integration.cross-module has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent integration concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.integration.cross-module:repository-context`, and adjacent integration ownership stays outside the verdict.
