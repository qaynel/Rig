# testing.integration.internal-contract - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `internal-contract-integration-context` evaluates `integration.internal-contract`. Given integration.internal-contract has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent integration concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.integration.internal-contract:repository-context`, and adjacent integration ownership stays outside the verdict.
