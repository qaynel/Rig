# testing.integration.internal-contract - slice property-floor

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `property-floor` slice holds ownership at `integration.internal-contract` and prevents none from being silently absorbed. It activates `internal-contract-source-code-boundary`, `internal-contract-evidence-receipt` and reports a named coverage gap when its repository binding or required dependency is unavailable.
