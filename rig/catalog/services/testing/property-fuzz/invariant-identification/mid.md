# testing.property-fuzz.invariant-identification - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `invariant-identification-property-fuzz-context` evaluates `property-fuzz.invariant-identification`. Given property-fuzz.invariant-identification has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent property fuzz concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.property-fuzz.invariant-identification:repository-context`, and adjacent property fuzz ownership stays outside the verdict.
