# testing.property-fuzz.fuzz-input-generation - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `fuzz-input-generation-property-fuzz-context` evaluates `property-fuzz.fuzz-input-generation`. Given property-fuzz.fuzz-input-generation has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent property fuzz concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.property-fuzz.fuzz-input-generation:repository-context`, and adjacent property fuzz ownership stays outside the verdict.
