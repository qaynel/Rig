# testing.maintenance.dead-test-pruning - slice property-floor

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `property-floor` slice holds ownership at `maintenance.dead-test-pruning` and prevents none from being silently absorbed. It activates `dead-test-pruning-source-code-boundary`, `dead-test-pruning-evidence-receipt` and reports a named coverage gap when its repository binding or required dependency is unavailable.
