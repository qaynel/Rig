# testing.maintenance.dead-test-pruning - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

dead test pruning is the sole catalogue owner of `maintenance.dead-test-pruning`. It must not absorb other maintenance concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `maintenance.dead-test-pruning`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.maintenance.dead-test-pruning:policy-boundary. Given source-code is present and maintenance.dead-test-pruning is selected, pass only when the repository binding evaluates maintenance.dead-test-pruning and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent maintenance concern.
