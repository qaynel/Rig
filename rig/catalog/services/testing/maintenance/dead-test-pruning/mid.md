# testing.maintenance.dead-test-pruning - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `dead-test-pruning-maintenance-context` evaluates `maintenance.dead-test-pruning`. Given maintenance.dead-test-pruning has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent maintenance concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.maintenance.dead-test-pruning:repository-context`, and adjacent maintenance ownership stays outside the verdict.
