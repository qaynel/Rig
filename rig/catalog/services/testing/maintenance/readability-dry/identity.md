# testing.maintenance.readability-dry - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

readability dry is the sole catalogue owner of `maintenance.readability-dry`. It must not absorb other maintenance concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `maintenance.readability-dry`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.maintenance.readability-dry:policy-boundary. Given source-code is present and maintenance.readability-dry is selected, pass only when the repository binding evaluates maintenance.readability-dry and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent maintenance concern.
