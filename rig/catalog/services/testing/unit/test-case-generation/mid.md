# testing.unit.test-case-generation - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `test-case-generation-unit-context` evaluates `unit.test-case-generation`. Given unit.test-case-generation has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent unit concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.unit.test-case-generation:repository-context`, and adjacent unit ownership stays outside the verdict.
