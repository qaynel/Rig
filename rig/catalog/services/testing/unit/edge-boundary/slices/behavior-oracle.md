# testing.unit.edge-boundary - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming edge boundary result from a real policy violation. It activates `edge-boundary-source-code-boundary`, `edge-boundary-unit-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
