# testing.unit.mocking-strategy - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

mocking strategy is the sole catalogue owner of `unit.mocking-strategy`. It must not absorb other unit concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `unit.mocking-strategy`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.unit.mocking-strategy:policy-boundary. Given source-code is present and unit.mocking-strategy is selected, pass only when the repository binding evaluates unit.mocking-strategy and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent unit concern.
