# testing.strategy.coverage-gap - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

coverage gap is the sole catalogue owner of `strategy.coverage-gap`. It must not absorb other strategy concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `strategy.coverage-gap`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.strategy.coverage-gap:policy-boundary. Given source-code is present and strategy.coverage-gap is selected, pass only when the repository binding evaluates strategy.coverage-gap and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent strategy concern.
