# testing.strategy.pyramid-balance - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming pyramid balance result from a real policy violation. It activates `pyramid-balance-source-code-boundary`, `pyramid-balance-strategy-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
