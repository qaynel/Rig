# testing.strategy.pyramid-balance - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `pyramid-balance-strategy-context` evaluates `strategy.pyramid-balance`. Given strategy.pyramid-balance has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent strategy concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.strategy.pyramid-balance:repository-context`, and adjacent strategy ownership stays outside the verdict.
