# testing.strategy.pyramid-balance - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

pyramid balance is the sole catalogue owner of `strategy.pyramid-balance`. It must not absorb other strategy concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `strategy.pyramid-balance`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.strategy.pyramid-balance:policy-boundary. Given source-code is present and strategy.pyramid-balance is selected, pass only when the repository binding evaluates strategy.pyramid-balance and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent strategy concern.
