# testing.strategy.risk-prioritization - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

risk prioritization is the sole catalogue owner of `strategy.risk-prioritization`. It must not absorb other strategy concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `strategy.risk-prioritization`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.strategy.risk-prioritization:policy-boundary. Given source-code is present and strategy.risk-prioritization is selected, pass only when the repository binding evaluates strategy.risk-prioritization and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent strategy concern.
