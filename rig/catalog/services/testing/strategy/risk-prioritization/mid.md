# testing.strategy.risk-prioritization - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `test-priority-ranking-strategy-context` evaluates `strategy.risk-prioritization`. Given strategy.risk-prioritization has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent strategy concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.strategy.risk-prioritization:repository-context`, and adjacent strategy ownership stays outside the verdict.
