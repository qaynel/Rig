# testing.regression.snapshot-golden - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

snapshot golden is the sole catalogue owner of `regression.snapshot-golden`. It must not absorb other regression concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `regression.snapshot-golden`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.regression.snapshot-golden:policy-boundary. Given source-code is present and regression.snapshot-golden is selected, pass only when the repository binding evaluates regression.snapshot-golden and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent regression concern.
