# testing.regression.suite-curation - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

suite curation is the sole catalogue owner of `regression.suite-curation`. It must not absorb other regression concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `regression.suite-curation`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.regression.suite-curation:policy-boundary. Given source-code is present and regression.suite-curation is selected, pass only when the repository binding evaluates regression.suite-curation and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent regression concern.
