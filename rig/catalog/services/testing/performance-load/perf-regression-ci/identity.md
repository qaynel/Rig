# testing.performance-load.perf-regression-ci - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

perf regression ci is the sole catalogue owner of `performance-load.perf-regression-ci`. It must not absorb other performance load concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `performance-load.perf-regression-ci`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.performance-load.perf-regression-ci:policy-boundary. Given source-code is present and performance-load.perf-regression-ci is selected, pass only when the repository binding evaluates performance-load.perf-regression-ci and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent performance load concern.
