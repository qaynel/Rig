# testing.performance-load.load-stress-scripts - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `load-stress-scripts-performance-load-context` evaluates `perf-load-test-authoring`, `load-stress-scripts`. Given perf-load-test-authoring has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent performance load concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.performance-load.load-stress-scripts:repository-context`, and adjacent performance load ownership stays outside the verdict.
