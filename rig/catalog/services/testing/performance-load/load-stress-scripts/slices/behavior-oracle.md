# testing.performance-load.load-stress-scripts - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming load stress scripts result from a real policy violation. It activates `load-stress-scripts-source-code-boundary`, `load-stress-scripts-performance-load-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
