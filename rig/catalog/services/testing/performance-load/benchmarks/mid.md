# testing.performance-load.benchmarks - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `benchmarks-performance-load-context` evaluates `performance-load.benchmarks`. Given performance-load.benchmarks has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent performance load concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:testing.performance-load.benchmarks:repository-context`, and adjacent performance load ownership stays outside the verdict.
