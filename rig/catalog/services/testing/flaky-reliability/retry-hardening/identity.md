# testing.flaky-reliability.retry-hardening - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

retry hardening is the sole catalogue owner of `flaky-reliability.retry-hardening`. It must not absorb other flaky reliability concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `flaky-reliability.retry-hardening`, the result is a named nonzero coverage gap.

Acceptance target: fixture:testing.flaky-reliability.retry-hardening:policy-boundary. Given source-code is present and flaky-reliability.retry-hardening is selected, pass only when the repository binding evaluates flaky-reliability.retry-hardening and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent flaky reliability concern.
