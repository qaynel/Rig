# infrastructure.environment-config.config-drift - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `config-drift-environment-config-context` evaluates `environment-config.config-drift`. Given environment-config.config-drift has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent environment config concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.environment-config.config-drift:repository-context`, and adjacent environment config ownership stays outside the verdict.
