# infrastructure.cost.rightsizing - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming rightsizing result from a real policy violation. It activates `rightsizing-source-code-boundary`, `rightsizing-cost-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
