# infrastructure.cost.utilization-analysis - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `utilization-analysis-cost-context` evaluates `cost.utilization-analysis`. Given cost.utilization-analysis has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent cost concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.cost.utilization-analysis:repository-context`, and adjacent cost ownership stays outside the verdict.
