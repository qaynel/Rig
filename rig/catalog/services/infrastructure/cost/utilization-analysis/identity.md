# infrastructure.cost.utilization-analysis - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

utilization analysis is the sole catalogue owner of `cost.utilization-analysis`. It must not absorb other cost concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `cost.utilization-analysis`, the result is a named nonzero coverage gap.

Acceptance target: fixture:infrastructure.cost.utilization-analysis:policy-boundary. Given source-code is present and cost.utilization-analysis is selected, pass only when the repository binding evaluates cost.utilization-analysis and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent cost concern.
