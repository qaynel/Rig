# infrastructure.cost.anomaly-detection - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

anomaly detection is the sole catalogue owner of `cost.anomaly-detection`. It must not absorb other cost concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `cost.anomaly-detection`, the result is a named nonzero coverage gap.

Acceptance target: fixture:infrastructure.cost.anomaly-detection:policy-boundary. Given source-code is present and cost.anomaly-detection is selected, pass only when the repository binding evaluates cost.anomaly-detection and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent cost concern.
