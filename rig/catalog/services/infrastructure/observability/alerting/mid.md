# infrastructure.observability.alerting - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `alerting-observability-context` evaluates `observability.alerting`. Given observability.alerting has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent observability concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.observability.alerting:repository-context`, and adjacent observability ownership stays outside the verdict.
