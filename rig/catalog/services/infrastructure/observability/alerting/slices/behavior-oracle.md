# infrastructure.observability.alerting - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming alerting result from a real policy violation. It activates `alerting-source-code-boundary`, `alerting-observability-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
