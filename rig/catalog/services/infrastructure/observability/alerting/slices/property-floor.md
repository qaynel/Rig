# infrastructure.observability.alerting - slice property-floor

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `property-floor` slice holds ownership at `observability.alerting` and prevents none from being silently absorbed. It activates `alerting-source-code-boundary`, `alerting-evidence-receipt` and reports a named coverage gap when its repository binding or required dependency is unavailable.
