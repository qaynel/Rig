# infrastructure.observability.metrics-dashboards - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

metrics dashboards is the sole catalogue owner of `observability.metrics-dashboards`. It must not absorb other observability concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `observability.metrics-dashboards`, the result is a named nonzero coverage gap.

Acceptance target: fixture:infrastructure.observability.metrics-dashboards:policy-boundary. Given source-code is present and observability.metrics-dashboards is selected, pass only when the repository binding evaluates observability.metrics-dashboards and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent observability concern.
