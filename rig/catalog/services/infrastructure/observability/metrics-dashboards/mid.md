# infrastructure.observability.metrics-dashboards - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `metrics-dashboards-observability-context` evaluates `observability.metrics-dashboards`. Given observability.metrics-dashboards has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent observability concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.observability.metrics-dashboards:repository-context`, and adjacent observability ownership stays outside the verdict.
