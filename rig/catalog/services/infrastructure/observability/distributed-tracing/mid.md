# infrastructure.observability.distributed-tracing - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `distributed-tracing-observability-context` evaluates `observability.distributed-tracing`. Given observability.distributed-tracing has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent observability concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.observability.distributed-tracing:repository-context`, and adjacent observability ownership stays outside the verdict.
