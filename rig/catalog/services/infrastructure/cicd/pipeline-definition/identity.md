# infrastructure.cicd.pipeline-definition - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

pipeline definition is the sole catalogue owner of `cicd.pipeline-definition`. It must not absorb other cicd concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `cicd.pipeline-definition`, the result is a named nonzero coverage gap.

Acceptance target: fixture:infrastructure.cicd.pipeline-definition:policy-boundary. Given source-code is present and cicd.pipeline-definition is selected, pass only when the repository binding evaluates cicd.pipeline-definition and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent cicd concern.
