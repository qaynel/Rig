# infrastructure.cicd.release-gating - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `release-gating-cicd-context` evaluates `cicd.release-gating`. Given cicd.release-gating has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent cicd concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.cicd.release-gating:repository-context`, and adjacent cicd ownership stays outside the verdict.
