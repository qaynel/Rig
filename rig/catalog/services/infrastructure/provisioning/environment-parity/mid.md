# infrastructure.provisioning.environment-parity - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `environment-parity-provisioning-context` evaluates `provisioning.environment-parity`. Given provisioning.environment-parity has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent provisioning concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.provisioning.environment-parity:repository-context`, and adjacent provisioning ownership stays outside the verdict.
