# infrastructure.provisioning.environment-parity - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

environment parity is the sole catalogue owner of `provisioning.environment-parity`. It must not absorb other provisioning concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `provisioning.environment-parity`, the result is a named nonzero coverage gap.

Acceptance target: fixture:infrastructure.provisioning.environment-parity:policy-boundary. Given source-code is present and provisioning.environment-parity is selected, pass only when the repository binding evaluates provisioning.environment-parity and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent provisioning concern.
