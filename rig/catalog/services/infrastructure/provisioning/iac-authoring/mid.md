# infrastructure.provisioning.iac-authoring - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `iac-authoring-provisioning-context` evaluates `provisioning.iac-authoring`. Given provisioning.iac-authoring has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent provisioning concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.provisioning.iac-authoring:repository-context`, and adjacent provisioning ownership stays outside the verdict.
