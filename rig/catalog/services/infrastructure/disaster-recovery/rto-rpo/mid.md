# infrastructure.disaster-recovery.rto-rpo - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `rto-rpo-disaster-recovery-context` evaluates `disaster-recovery.rto-rpo`. Given disaster-recovery.rto-rpo has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent disaster recovery concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.disaster-recovery.rto-rpo:repository-context`, and adjacent disaster recovery ownership stays outside the verdict.
