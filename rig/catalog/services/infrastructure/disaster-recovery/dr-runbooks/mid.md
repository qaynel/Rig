# infrastructure.disaster-recovery.dr-runbooks - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `dr-runbooks-disaster-recovery-context` evaluates `disaster-recovery.dr-runbooks`. Given disaster-recovery.dr-runbooks has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent disaster recovery concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.disaster-recovery.dr-runbooks:repository-context`, and adjacent disaster recovery ownership stays outside the verdict.
