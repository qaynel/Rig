# infrastructure.storage.db-provisioning - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `db-provisioning-storage-context` evaluates `storage.db-provisioning`. Given storage.db-provisioning has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent storage concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.storage.db-provisioning:repository-context`, and adjacent storage ownership stays outside the verdict.
