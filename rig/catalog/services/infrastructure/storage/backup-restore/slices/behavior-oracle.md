# infrastructure.storage.backup-restore - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming backup restore result from a real policy violation. It activates `backup-restore-source-code-boundary`, `backup-restore-storage-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
