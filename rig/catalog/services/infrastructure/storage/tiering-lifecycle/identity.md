# infrastructure.storage.tiering-lifecycle - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

tiering lifecycle is the sole catalogue owner of `storage.tiering-lifecycle`. It must not absorb other storage concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `storage.tiering-lifecycle`, the result is a named nonzero coverage gap.

Acceptance target: fixture:infrastructure.storage.tiering-lifecycle:policy-boundary. Given source-code is present and storage.tiering-lifecycle is selected, pass only when the repository binding evaluates storage.tiering-lifecycle and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent storage concern.
