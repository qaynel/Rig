# infrastructure.storage.tiering-lifecycle - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `tiering-lifecycle-storage-context` evaluates `storage.tiering-lifecycle`. Given storage.tiering-lifecycle has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent storage concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.storage.tiering-lifecycle:repository-context`, and adjacent storage ownership stays outside the verdict.
