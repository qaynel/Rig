# infrastructure.networking.load-balancer-ingress - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming load balancer ingress result from a real policy violation. It activates `load-balancer-ingress-source-code-boundary`, `load-balancer-ingress-networking-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
