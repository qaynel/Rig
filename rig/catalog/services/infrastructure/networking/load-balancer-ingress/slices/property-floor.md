# infrastructure.networking.load-balancer-ingress - slice property-floor

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `property-floor` slice holds ownership at `networking.load-balancer-ingress` and prevents none from being silently absorbed. It activates `load-balancer-ingress-source-code-boundary`, `load-balancer-ingress-evidence-receipt` and reports a named coverage gap when its repository binding or required dependency is unavailable.
