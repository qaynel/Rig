# infrastructure.container.k8s-helm - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `k8s-helm-container-context` evaluates `container.k8s-helm`. Given container.k8s-helm has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent container concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.container.k8s-helm:repository-context`, and adjacent container ownership stays outside the verdict.
