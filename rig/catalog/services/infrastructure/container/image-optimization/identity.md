# infrastructure.container.image-optimization - identity

Policy grade. This is generic baseline practice, not a claim of repository-tailored Context or Evidence coverage.

image optimization is the sole catalogue owner of `container.image-optimization`. It must not absorb other container concerns. Profile evidence is `source-code`.

Disposition: convention. Dependencies: none. First wire reuses an existing repository command, then a native platform feature, then an installed dependency; if none observes `container.image-optimization`, the result is a named nonzero coverage gap.

Acceptance target: fixture:infrastructure.container.image-optimization:policy-boundary. Given source-code is present and container.image-optimization is selected, pass only when the repository binding evaluates container.image-optimization and returns zero; fail when the binding is missing, unreadable, exits nonzero, or claims an adjacent container concern.
