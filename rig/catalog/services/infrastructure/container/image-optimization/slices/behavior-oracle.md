# infrastructure.container.image-optimization - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming image optimization result from a real policy violation. It activates `image-optimization-source-code-boundary`, `image-optimization-container-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
