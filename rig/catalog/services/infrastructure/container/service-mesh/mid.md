# infrastructure.container.service-mesh - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `service-mesh-container-context` evaluates `container.service-mesh`. Given container.service-mesh has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent container concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.container.service-mesh:repository-context`, and adjacent container ownership stays outside the verdict.
