# infrastructure.container.service-mesh - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming service mesh result from a real policy violation. It activates `service-mesh-source-code-boundary`, `service-mesh-container-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
