# infrastructure.scaling-reliability.chaos-engineering - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming chaos engineering result from a real policy violation. It activates `chaos-engineering-source-code-boundary`, `chaos-engineering-scaling-reliability-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
