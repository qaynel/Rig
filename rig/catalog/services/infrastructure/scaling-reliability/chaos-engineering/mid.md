# infrastructure.scaling-reliability.chaos-engineering - mid

Grade: mid (Context target). Until the named local evidence is wired, assurance remains Policy-grade generic baseline practice rather than repository-tailored coverage.

After the minimal boundary passes, `chaos-engineering-scaling-reliability-context` evaluates `scaling-reliability.chaos-engineering`. Given scaling-reliability.chaos-engineering has a repository-specific convention and evidence source, it passes only when the result names the consulted path or command and excludes an adjacent scaling reliability concern; it fails when the result infers local convention without an inspectable source. The target is `fixture:infrastructure.scaling-reliability.chaos-engineering:repository-context`, and adjacent scaling reliability ownership stays outside the verdict.
