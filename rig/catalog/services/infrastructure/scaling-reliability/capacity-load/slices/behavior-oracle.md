# infrastructure.scaling-reliability.capacity-load - slice behavior-oracle

Policy-grade generic baseline practice, not repository-tailored Context or Evidence coverage.

The `behavior-oracle` slice requires an example that distinguishes a conforming capacity load result from a real policy violation. It activates `capacity-load-source-code-boundary`, `capacity-load-scaling-reliability-context` and reports a named coverage gap when its repository binding or required dependency is unavailable.
