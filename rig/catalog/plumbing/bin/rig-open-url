#!/usr/bin/env bash
# rig-open-url — cross-platform URL opener
#
# Usage: rig-open-url <url>
set -euo pipefail

URL="${1:?Usage: rig-open-url <url>}"

case "$(uname -s)" in
  Darwin)  open "$URL" ;;
  Linux)   xdg-open "$URL" 2>/dev/null || echo "$URL" ;;
  MINGW*|MSYS*|CYGWIN*) start "$URL" ;;
  *)       echo "$URL" ;;
esac
